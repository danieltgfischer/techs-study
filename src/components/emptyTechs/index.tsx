import React, { FC } from 'react';
import { Container, Title } from './styles';

export const EmptyTechContainer: FC = () => {
	return (
		<Container>
			<Title>Nenhuma tecnologia adicionada ainda</Title>
		</Container>
	);
};
